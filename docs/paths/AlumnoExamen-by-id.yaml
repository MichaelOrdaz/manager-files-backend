get:
  tags: [Alumno]
  summary: "Get - AlumnoExamen"
  operationId: getAlumnoExamen
  description: "Obtiene el detalle de un examen con su calificación"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/path/examenId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/AlumnoExamen-by-id.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true