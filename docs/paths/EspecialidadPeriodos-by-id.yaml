get:
  tags: [EspecialidadPeriodo]
  summary: "Get - EspecialidadPeriodo"
  operationId: getEspecialidadPeriodo
  description: "Obtiene la especialidades periodos de la base de datos con el id proporcionado"
  parameters:
    - $ref: "../parameters/path/especialidadPeriodoId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content: 
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: "../schemas/EspecialidadPeriodo.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true
    422:
      $ref: "../responses/error422.yaml"
    404:
      $ref: "../responses/error404.yaml"
    401:
      $ref: "../responses/error401.yaml"