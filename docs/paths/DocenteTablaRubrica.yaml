get:
  tags: [Docentes]
  summary: "get - DocenteTablaRubrica"
  operationId: getDocenteTablaRubrica
  description: "Regresa una lista de usuarios con sus porcentajes de la rubrica"
  parameters:
    - $ref: "../parameters/path/docenteMateriaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocenteTablaRubrica.yaml"
                maxItems: 10
              message:
                type: string
              success:
                type: boolean
                example: true