get:
  tags: [Docentes]
  summary: "get - DocenteTablaCalificacion"
  operationId: getDocenteTablaCalificacion
  description: "Obtiene un objeto con los atributos columnas y filas para la tabla calificación"
  parameters:
    - $ref: "../parameters/path/docenteMateriaId.yaml"
    - $ref: "../parameters/query/fechaInicio.yaml"
    - $ref: "../parameters/query/fechaFin.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocenteTablaCalificacion.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true