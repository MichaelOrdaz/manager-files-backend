get:
  tags: [PreguntasTipo]
  summary: "List - Tipos de pregunta"
  operationId: listPreguntasTipo
  description: "Lista los tipos de preguntas de un examen o encuesta"
  parameters:
    - $ref: "../parameters/query/page.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/PreguntasTipo.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
              links:
                $ref: "../meta-datos/links.yaml"
              meta:
                $ref: "../meta-datos/paginacion.yaml"
