get:
  tags: [MaterialesDidacticos]
  summary: "Search - MaterialesDidacticos"
  operationId: searchMaterialesDidacticos
  description: "Filtra de repositorios material didáctico"
  parameters:
    - $ref: "../parameters/query/page.yaml"
    - $ref: "../parameters/query/temaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/MaterialesDidacticosJson.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
              links:
                $ref: "../meta-datos/links.yaml"
              meta:
                $ref: "../meta-datos/paginacion.yaml"
