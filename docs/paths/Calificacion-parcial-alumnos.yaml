get:
  tags: [CalificacionParcial]
  summary: "list - CalificacionParcial"
  operationId: listCalificacionParcialAlumnos
  description: "Obtiene una lista de alumnos con sus calificaciones parciales"
  parameters:
    - $ref: "../parameters/path/especialidadPeriodoGrupoId.yaml"
    - $ref: "../parameters/path/materiaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Calificacion-parcial-listAlumnos.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true