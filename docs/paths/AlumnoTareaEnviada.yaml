get:
  tags: [Alumno]
  summary: "List - AlumnoTareasEnviadas"
  operationId: listAlumnoTareasEnviadas
  description: "Lista tareas a enviadas por el alumno con el status de la tarea enviada"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
    - $ref: "../parameters/query/temaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/AlumnoTareaEnviada.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true