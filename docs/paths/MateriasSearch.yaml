get:
  tags: [Materias]
  summary: "search - Materias"
  operationId: searchMaterias
  description: "filtra las materias"
  parameters:
    - $ref: "../parameters/query/page.yaml"
    - $ref: "../parameters/query/periodoId.yaml"
    - $ref: "../parameters/query/especialidadId.yaml"
    - $ref: "../parameters/query/nombre.yaml"
    - $ref: "../parameters/query/docenteId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/MateriasSearch.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true