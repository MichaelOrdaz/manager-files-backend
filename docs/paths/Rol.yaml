get:
  tags: [Rol]
  summary: "List - roles"
  operationId: listRoles
  description: "Lista Roles"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Rol.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
    422:
      $ref: "../responses/error422.yaml"
    404:
      $ref: "../responses/error404.yaml"
    401:
      $ref: "../responses/error401.yaml"
