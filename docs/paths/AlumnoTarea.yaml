get:
  tags: [Alumno]
  summary: "List - AlumnoTareas"
  operationId: listAlumnoTareas
  description: "Lista tareas a entregar para el alumno"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
    - $ref: "../parameters/query/temaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/AlumnoTarea.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true