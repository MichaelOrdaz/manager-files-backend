get:
  tags: [CalificacionParcial]
  summary: "get - boletaAlumno"
  operationId: getBoletaAlumno
  description: "Obtiene la url del pdf boleta para un alumno"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/boleta-alumno.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true