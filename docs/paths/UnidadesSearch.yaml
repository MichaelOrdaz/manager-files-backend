get:
  tags: [Unidades]
  summary: "search - Unidades"
  operationId: searchUnidades
  description: "filtra las unidades"
  parameters:
    - $ref: "../parameters/query/materiaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Unidades.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true