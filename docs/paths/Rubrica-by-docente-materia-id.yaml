get:
  tags: [Rubrica]
  summary: "Get - RubricaPorDocenterMateriaID"
  operationId: getRubricaPorDocenterMateriaID
  description: "Obtiene una rubrica por docente materia id"
  parameters:
    - $ref: "../parameters/path/docenteMateriaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Rubrica.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true