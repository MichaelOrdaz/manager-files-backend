post:
  tags: [Eventos]
  summary: "Create - Evento"
  operationId: createEvento
  description: "Crea un nuevo evento<br>
  <ul>
    <li><b>dirigido_a :</b> Sus valores son:
      <ul>
        <li>los nombres de <b>roles</b></li>
        <li><b>Todos</b> Se emite a todos los usuarios sin distinción</li>
        <li><b>Grupos</b> para EspecialidadPeriodoGrupo</li>
        <li><b>Materias</b> para DocenteMateria</li>
      </ul>
    </li>
    <li><b>dirigido_id:</b> Es el id correspondiente a EspecialidadPeriodoGrupo o DocenteMateria
      <ul>
        <li> Grupos - especialidad_periodo_grupo_id </li>
        <li> Materias - docente_materia_id </li>
      </ul>
      <li> <b>NOTA:</b> solo pone este datos si el evento va dirigida a <b>Grupos</b> o <b>Materias</b> </li>
    </li>
  </ul>"
  requestBody:
    {
      description: "Petición multipart/form-data",
      content:
        { multipart/form-data: { schema: { $ref: "../schemas/EventosMultiPart.yaml" } } },
    }
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json: { schema: { $ref: "../schemas/EventosJson.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
get:
  tags: [Eventos]
  summary: "List - Eventos"
  operationId: listEventos
  description: "Lista todos los eventos"
  parameters:
    - $ref: "../parameters/query/page.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/EventosJson.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
              links:
                $ref: "../meta-datos/links.yaml"
              meta:
                $ref: "../meta-datos/paginacion.yaml"
