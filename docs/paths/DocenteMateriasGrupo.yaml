get:
  tags: [Docentes]
  summary: "get - Docente Materias Grupos"
  operationId: getMateriasGrupo
  description: "Devuelve la relación de las materias y grupos a los que un Docente imparte clases"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocenteMateriasGrupo.yaml"
                maxItems: 10
              message:
                type: string
              success:
                type: boolean
                example: true