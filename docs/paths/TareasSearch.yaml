get:
  tags: [Tareas]
  summary: "search - Tareas"
  operationId: searchTareas
  description: "filtra las tareas"
  parameters:
    - $ref: "../parameters/query/page.yaml"
    - $ref: "../parameters/query/nombre.yaml"
    - $ref: "../parameters/query/unidadId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
    - $ref: "../parameters/query/periodoIdByGrupo.yaml"
    - $ref: "../parameters/query/periodoIdByMateria.yaml"
    - $ref: "../parameters/query/temaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/TareasJson.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true