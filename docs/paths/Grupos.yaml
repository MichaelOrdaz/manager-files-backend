post:
  tags: [Grupos]
  summary: "Create - Grupos"
  operationId: createGrupo
  description: "Crea un nuevo grupo"
  requestBody: { description: "Petición en formato json", required: true, content: { application/json: { schema: { $ref: "../schemas/Grupos.yaml" } } } }
  responses: { 200: { description: "Estructura básica de la respuesta", content: { application/json: { schema: { $ref: "../schemas/Grupos.yaml" } } } }, 422: { $ref: "../responses/error422.yaml" }, 404: { $ref: "../responses/error404.yaml" }, 401: { $ref: "../responses/error401.yaml" } }
get:
  tags: [Grupos]
  summary: "List - Grupos"
  operationId: listGrupos
  description: "Lista grupos"
  parameters:
    - $ref: "../parameters/query/page.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Grupos.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
              links:
                $ref: "../meta-datos/links.yaml"
              meta:
                $ref: "../meta-datos/paginacion.yaml"
