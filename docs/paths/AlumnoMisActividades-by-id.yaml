get:
  tags: [Alumno]
  summary: "Get - MisActividades"
  operationId: getMisActividades
  description: "Obtiene las tareas y los examenes a entregar para el alumno."
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
    - $ref: "../parameters/query/fecha.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/AlumnoMisActividades-by-id.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true
