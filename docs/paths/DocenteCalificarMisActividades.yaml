put:
  tags: [Docentes]
  summary: "update - DocenteCalificarMisActividades"
  operationId: updateDocenteCalificarMisActividades
  description: "Actualiza una activad academica del alumno"
  parameters:
    - $ref: "../parameters/path/id.yaml"
  requestBody:
    {
      description: "Petición en formato json<br><br>
        Actualiza una actividades academica del alumno <br>
        <b>por el campo calificacion</b><br>
        <ul>
          <li> para una tarea es la calificación </li>
          <li> para un examen es la calificacion del examen </li>
          <li> para una tutoria es la respuesta que da el profesor </li>
          <li> para asistencia es un booleano asistio true, no asistio false </li>
        </ul>
        <b>el parametro id</b><br>
        <ul>
          <li>para tarea corresponde a tarea_enviada_id </li>
          <li>para examen corresponde a examen_calificacion_id </li>
          <li>para tutoria corresponde a tutoria_id </li>
          <li>para asistencia corresponde a asistencia_alumno_id </li>
        </ul>
      <b>asignado</b> es el tipo de actividad, valores validos : Tarea, Examen, Tutoria, Asistencia
      ",
      content:
        {
          application/json: { schema: { $ref: "../schemas/DocenteCalificarMisActividades.yaml" } },
        },
    }
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/DocenteCalificarMisActividades.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }