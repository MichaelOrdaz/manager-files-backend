get:
  tags: [Docentes]
  summary: "get - DocenteTablaCalificacionAlumno"
  operationId: getDocenteTablaCalificacionAlumno
  description: "Obtiene un objeto con los atributos alumno y actividades"
  parameters:
    - $ref: "../parameters/path/docenteMateriaId.yaml"
    - $ref: "../parameters/path/usuarioId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocenteTablaCalificacionAlumno.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true