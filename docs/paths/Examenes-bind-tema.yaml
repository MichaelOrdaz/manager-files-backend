post:
  tags: [Examenes]
  summary: "Bind - Examenes"
  operationId: bindExamenes
  description: "Relaciona un examen con un tema por sus respectivos id"
  parameters:
    - $ref: "../parameters/path/examenId.yaml"
    - $ref: "../parameters/path/temaId.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/ExamenesBindingTema.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
