get:
  tags: [TiposExamenes]
  summary: "Get - Tipo Examen"
  operationId: getTipoExamen
  description: "Obtiene un Tipo de examen pasando un examen_tipo_id"
  parameters:
    - $ref: "../parameters/path/examenTipoId.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/ExamenesTipo.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
