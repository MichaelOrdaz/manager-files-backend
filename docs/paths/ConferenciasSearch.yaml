get:
  tags: [Conferencias]
  summary: "Search - Conferencias"
  operationId: searchConferencias
  description: "Filtra las Conferencias"
  parameters:
    - $ref: "../parameters/query/page.yaml"
    - $ref: "../parameters/query/docenteId.yaml"
    - $ref: "../parameters/query/grupoId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
    - $ref: "../parameters/query/conferenciaStatus.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Conferencias.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
