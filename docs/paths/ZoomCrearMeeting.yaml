post:
  tags: [Zoom]
  summary: "create - crearZoomMeeting"
  operationId: crearZoomMeeting
  description: "Crea un ZoomMeeting con los datos de la tabla de conferencia"
  requestBody:
    {
      description: "Petición en formato json",
      content:
        {
          application/json:
            { schema: { $ref: "../schemas/ZoomConferencias.yaml" } },
        },
    }
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/ZoomConferencias.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }