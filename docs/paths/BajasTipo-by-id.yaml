get:
  tags: [BajasTipo]
  summary: "Get - BajasTipo"
  operationId: getBajasTipo
  description: "Obtiene un tipo de baja"
  parameters:
    - $ref: "../parameters/path/bajaTipoId.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/BajasTipo.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }