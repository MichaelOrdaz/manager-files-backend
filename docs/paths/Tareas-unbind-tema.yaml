delete:
  tags: [Tareas]
  summary: "Unbind - Tarea Tema"
  operationId: unbindTareasTemas
  description: "Elimina la relacion de una tarea con un tema"
  parameters:
    - $ref: "../parameters/path/tareaId.yaml"
    - $ref: "../parameters/path/temaId.yaml"
  responses:
    {
      201:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/TareasBindingTema.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
