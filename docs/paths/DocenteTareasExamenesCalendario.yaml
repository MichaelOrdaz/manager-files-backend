get:
  tags: [Docentes]
  summary: "get Tareas Examenes - Calendario"
  operationId: getTareasExamenesCalendario
  description: "Obtiene todos los examenes y tareas de un docente"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocenteTareasExamenesCalendario.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true