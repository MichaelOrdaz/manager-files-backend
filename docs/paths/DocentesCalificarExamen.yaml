get:
  tags: [Docentes]
  summary: "List - DocenteCalificarExamen"
  operationId: listDocenteCalificarExamen
  description: "Lista examenes a calificar de los alumnos - el objeto principal es el alumno"
  parameters:
    - $ref: "../parameters/path/examenId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocentesCalificarExamen.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
              links:
                $ref: "../meta-datos/links.yaml"
              meta:
                $ref: "../meta-datos/paginacion.yaml"
