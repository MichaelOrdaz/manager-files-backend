post:
  tags: [Docentes]
  summary: "CreateUpdate - DocenteAsistenciaGrupo"
  operationId: createUpdateAsistenciasGrupo
  description: "Recibe un array de asistencias para los alumnos en un conferencia"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
  requestBody:
    {
      description: "Petición en formato json",
      required: true,
      content:
        { application/json: { schema: { $ref: "../schemas/DocenteAsistenciaGrupoRequest.yaml" } } },
    }
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocenteAsistenciaResponse.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
