get:
  tags: [Rubrica]
  summary: "Get - Rubrica"
  operationId: getRubrica
  description: "Obtiene una rubrica por id"
  parameters:
    - $ref: "../parameters/path/rubricaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Rubrica.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true