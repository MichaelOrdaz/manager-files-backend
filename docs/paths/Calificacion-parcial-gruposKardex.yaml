get:
  tags: [CalificacionParcial]
  summary: "get - KardexGrupo"
  operationId: getKardexGrupo
  description: "Obtiene la url del zip de los kardex para un grupo"
  parameters:
    - $ref: "../parameters/path/especialidadPeriodoGrupoId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/kardex-alumno.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true