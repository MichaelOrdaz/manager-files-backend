post:
  tags: [DocentesMaterias]
  summary: "bind - Docente Materia"
  operationId: bindDocentesMaterias
  description: "Relaciona a un usuario de rol docente con una materia del mismo periodo, especialidad y especialidad y a un grupo"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/path/materiaId.yaml"
  requestBody: { description: "Petición en formato json", content: { application/json: { schema: { $ref: "../schemas/DocenteMateria.yaml" } } } }
  responses: { 200: { description: "Estructura básica de la respuesta", content: { application/json: { schema: { $ref: "../schemas/DocenteMateria.yaml" } } } }, 422: { $ref: "../responses/error422.yaml" }, 404: { $ref: "../responses/error404.yaml" }, 401: { $ref: "../responses/error401.yaml" } }