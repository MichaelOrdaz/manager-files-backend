get:
  tags: [Temas]
  summary: "search - Temas"
  operationId: searchTemas
  description: "filtra los temas"
  parameters:
    - $ref: "../parameters/query/unidadId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Temas.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true