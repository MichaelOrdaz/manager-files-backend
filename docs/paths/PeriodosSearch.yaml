get:
  tags: [Periodos]
  summary: "search - Periodos"
  operationId: searchPeriodos
  description: "filtra los periodos"
  parameters:
    - $ref: "../parameters/query/especialidadId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Periodos.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true