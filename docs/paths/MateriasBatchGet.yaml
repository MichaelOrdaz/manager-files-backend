get:
  tags: [Materias]
  summary: "BatchGet - Materias"
  operationId: BatchGetMaterias
  description: "Devuelve vista de árbol a partir de materias"
  parameters:
    - $ref: "../parameters/path/materiaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/MateriasBatchGet.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
