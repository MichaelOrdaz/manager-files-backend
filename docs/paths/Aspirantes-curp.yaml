get:
  tags: [Usuarios]
  summary: "Get - existeCurp"
  operationId: aspirantesComprobarCurp
  description: "Verifica si el curp ya fue registrado en la base de datos"
  parameters:
    - $ref: "../parameters/path/curp.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                {
                  schema:
                    { $ref: "../schemas/Aspirantes-curp.yaml" },
                },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }