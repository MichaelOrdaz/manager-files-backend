get:
  tags: [CalificacionParcial]
  summary: "get - ActaSegumientoEvaluativo"
  operationId: getActaSegumientoEvaluativo
  description: "Obtiene la url del acta del seguimiento evaluativo"
  parameters:
    - $ref: "../parameters/path/especialidadPeriodoGrupoId.yaml"
    - $ref: "../parameters/path/materiaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/acta-seguimiento.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true