get:
  tags: [Zoom]
  summary: "get - generarSignature"
  operationId: generarSignature
  description: "Crea un signature para la autentificación de la api zoom"
  parameters:
    - $ref: "../parameters/query/numberMeeting.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/ZoomGenerarSignature.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }