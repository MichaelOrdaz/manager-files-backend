get:
  tags: [Encuestas]
  summary: "Get - EncuestaResultados"
  operationId: getEncuestaResultados
  description: "Retorna el resultado de una encuesta"
  parameters:
    - $ref: "../parameters/path/encuestasId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/EncuestaResultado.yaml"
                maxItems: 10
              message:
                type: string
              success:
                type: boolean
                example: true
              links:
                $ref: "../meta-datos/links.yaml"
              meta:
                $ref: "../meta-datos/paginacion.yaml"

