get:
  tags: [Docentes]
  summary: "get - Listado de todos los alumnos del usuario docente"
  operationId: getListadoAlumnos
  description: "Devuelve los usuarios a los que el docente importe clases"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/Usuarios.yaml"
                maxItems: 10
              message:
                type: string
              success:
                type: boolean
                example: true