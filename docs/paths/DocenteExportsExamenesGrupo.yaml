get:
  tags: [Docentes]
  summary: "get - DocenteExportsExamenesGrupo"
  operationId: getDocenteExportsExamenesGrupo
  description: "Retorna la url del fichero zip de exceles donde se exportan los examenes"
  parameters:
    - $ref: "../parameters/path/docenteMateriaId.yaml"
    - $ref: "../parameters/query/fechaInicio.yaml"
    - $ref: "../parameters/query/fechaFin.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocenteExportsExamenesGrupo.yaml"
                maxItems: 10
              message:
                type: string
              success:
                type: boolean
                example: true