put:
  tags: [Docentes]
  summary: "Update - DocenteAsistenciaAlumno"
  operationId: updateDocenteAsistenciaAlumno
  description: "Actualiza la asistencia de un alumno para una conferencia"
  parameters:
    - in: path
      name: docente_id
      description: id del usuario de tipo docente
      schema:
        type: integer
        example: 1
      required: true
    - in: path
      name: alumno_id
      description: id del usuario de tipo alumno
      schema:
        type: integer
        example: 1
      required: true
  requestBody:
    {
      description: "Petición en formato json",
      required: true,
      content:
        { application/json: { schema: { $ref: "../schemas/DocenteAsistenciaAlumnoRequest.yaml" } } },
    }
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/DocenteAsistenciaResponse.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }