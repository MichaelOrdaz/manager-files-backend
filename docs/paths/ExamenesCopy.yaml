post:
  tags: [Examenes]
  summary: "Copy - Examenes"
  operationId: CopyExamenes
  description: "copia el examen por el id proporcionado en la url, y se copian tambien las preguntas que tenga relacionadas"
  parameters:
    - $ref: "../parameters/path/examenId.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/ExamenWithPreguntas.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
