get:
  tags: [Alumno]
  summary: "List - AlumnoExamenes"
  operationId: listAlumnoExamenes
  description: "Lista los examenes de un alumno"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/AlumnoExamen.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true