post:
  tags: [Usuarios]
  summary: "Finaliza el Timestamp usuario con examen"
  operationId: endTimestamp
  description: "cierra la marca de tiempo de cuanto comenzo a hacer el examen el usuario para verificar el tiempo restante"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/path/examenId.yaml"
  responses: { 200: { description: "Estructura básica de la respuesta", content: { application/json: { schema: { $ref: "../schemas/Timestamp.yaml" } } } }, 422: { $ref: "../responses/error422.yaml" }, 404: { $ref: "../responses/error404.yaml" }, 401: { $ref: "../responses/error401.yaml" } }