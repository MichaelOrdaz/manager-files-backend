get:
  tags: [DocentesMaterias]
  summary: "list - Docente Materia Grupos"
  operationId: listDocentesMateriasGrupos
  description: "Devuelve los grupos a los que puede asignar un examen un docente"
  parameters:
    - $ref: "../parameters/path/usuarioId.yaml"
    - $ref: "../parameters/path/materiaId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/DocentesMateria-Grupos.yaml"
                maxItems: 10
              message:
                type: string
              success:
                type: boolean
                example: true