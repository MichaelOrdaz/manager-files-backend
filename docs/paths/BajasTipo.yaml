get:
  tags: [BajasTipo]
  summary: "List - BajasTipo"
  operationId: listBajasTipo
  description: "Lista de los tipos de baja que se le pueden asignar a los alumnos"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/BajasTipo.yaml"
              message:
                type: string
              success:
                type: boolean
                example: true
    422:
      $ref: "../responses/error422.yaml"
    404:
      $ref: "../responses/error404.yaml"
    401:
      $ref: "../responses/error401.yaml"
