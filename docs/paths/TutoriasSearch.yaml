get:
  tags: [Tutorias]
  summary: "Search - Tutorias"
  operationId: searchTutorias
  description: "Filtra tutorías"
  parameters:
    - $ref: "../parameters/query/grupoId.yaml"
    - $ref: "../parameters/query/materiaId.yaml"
    - $ref: "../parameters/query/temaId.yaml"
    - $ref: "../parameters/query/docenteId.yaml"
    - $ref: "../parameters/query/materialId.yaml"
  responses:
    200:
      description: "Estructura básica de la respuesta"
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: array
                items:
                  $ref: "../schemas/TutoriasJson.yaml"
                maxItems: 25
              message:
                type: string
              success:
                type: boolean
                example: true
