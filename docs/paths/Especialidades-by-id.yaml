get:
  tags: [Especialidades]
  summary: "Get - Especialidades"
  operationId: getEspecialidad
  description: "Obtiene una especialidad"
  parameters:
    - $ref: "../parameters/path/especialidadId.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/EspecialidadesJson.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
post:
  tags: [Especialidades]
  summary: "Update - Especialidades"
  operationId: updateEspecialidad
  description: "Actualiza una especialidad"
  parameters:
    - $ref: "../parameters/path/especialidadId.yaml"
  requestBody:
    {
      description: "Petición multipart/form-data",
      content:
        {
          multipart/form-data:
            { schema: { $ref: "../schemas/EspecialidadesMultiPart.yaml" } },
        },
    }
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/EspecialidadesJson.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
delete:
  tags: [Especialidades]
  summary: "Delete - Especialidades"
  operationId: deleteEspecialidad
  description: "Borra una especialidad"
  parameters:
    - $ref: "../parameters/path/especialidadId.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                { schema: { $ref: "../schemas/EspecialidadesJson.yaml" } },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }