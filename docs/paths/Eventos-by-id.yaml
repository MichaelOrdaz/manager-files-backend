get:
  tags: [Eventos]
  summary: "Get - Evento"
  operationId: getEvento
  description: "Obtiene un evento pasando evento_id"
  parameters:
    - $ref: "../parameters/path/eventoId.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                {
                  schema:
                    { $ref: "../schemas/EventosJson.yaml" },
                },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
post:
  tags: [Eventos]
  summary: "Update - Evento"
  operationId: updateEvento
  description: "Actualiza un evento pasando un evento"
  parameters:
    - $ref: "../parameters/path/eventoId.yaml"
  requestBody:
    {
      description: "Petición multipart/form-data",
      content:
        {
          multipart/form-data:
            { schema: { $ref: "../schemas/EventosMultiPart.yaml" } },
        },
    }
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                {
                  schema:
                    { $ref: "../schemas/EventosJson.yaml" },
                },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }
delete:
  tags: [Eventos]
  summary: "Delete Evento"
  operationId: deleteEvento
  description: "Borra un evento pasando un evento_id"
  parameters:
    - $ref: "../parameters/path/eventoId.yaml"
  responses:
    {
      200:
        {
          description: "Estructura básica de la respuesta",
          content:
            {
              application/json:
                {
                  schema:
                    { $ref: "../schemas/EventosJson.yaml" },
                },
            },
        },
      422: { $ref: "../responses/error422.yaml" },
      404: { $ref: "../responses/error404.yaml" },
      401: { $ref: "../responses/error401.yaml" },
    }