type: object
properties:
  password:
    type: string
    format: password
    example: currentPass
  password_confirmation:
    type: string
    format: password
    example: currentPass
  new_password:
    type: string
    format: password
    example: newPassword

required:
  - password
  - password_confirmation
  - new_password