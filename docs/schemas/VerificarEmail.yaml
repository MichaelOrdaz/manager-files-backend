type: object
properties:
  email:
    type: string
    format: email
    example: correo@puller.mx
    writeOnly: true
  password:
    type: string
    format: password
    example: "12345"
    writeOnly: true

required:
  - email
  - password
