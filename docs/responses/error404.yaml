description: Elemento no encontrado
content:
  application/json:
    schema:
      type: object
      properties:
        errors:
          type: array
          items:
            type: string
            example: "Registro no encontrado"
        success:
          type: boolean
          example: false